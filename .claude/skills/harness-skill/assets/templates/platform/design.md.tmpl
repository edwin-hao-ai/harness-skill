---
version: alpha
name: {{project_name}} Design System
description: Visual identity and design tokens for {{project_name}}
colors:
  primary: "#{{primary_color_hex}}"
  secondary: "#{{secondary_color_hex}}"
  background: "#{{background_color_hex}}"
  surface: "#{{surface_color_hex}}"
  text: "#{{text_color_hex}}"
typography:
  h1:
    fontFamily: {{heading_font}}
    fontSize: 48px
    fontWeight: 700
    lineHeight: 1.2
  h2:
    fontFamily: {{heading_font}}
    fontSize: 36px
    fontWeight: 600
    lineHeight: 1.3
  h3:
    fontFamily: {{heading_font}}
    fontSize: 24px
    fontWeight: 600
    lineHeight: 1.4
  body:
    fontFamily: {{body_font}}
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.6
  caption:
    fontFamily: {{body_font}}
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.5
spacing:
  xs: 4px
  sm: 8px
  md: 16px
  lg: 24px
  xl: 32px
  gutter: 24px
rounded:
  sm: 4px
  md: 8px
  lg: 12px
  full: 9999px
---

# DESIGN.md · {{project_name}}

> 视觉设计系统文档（Google DESIGN.md 规范）：定义品牌视觉标识、设计令牌与样式准则。
> 产品需求见 [spec.md](spec.md)；技术架构见 [architecture.md](architecture.md)。

## Overview

{{design_overview}}

**品牌个性**：{{brand_personality}}

**目标用户**：{{target_users_names}}

**视觉感受**：{{visual_feeling}}

## Colors

{{colors_description}}

- **Primary (`#{{primary_color_hex}}`)**：{{primary_color_usage}}
- **Secondary (`#{{secondary_color_hex}}`)**：{{secondary_color_usage}}
- **Background (`#{{background_color_hex}}`)**：{{background_color_usage}}
- **Surface (`#{{surface_color_hex}}`)**：{{surface_color_usage}}
- **Text (`#{{text_color_hex}}`)**：{{text_color_usage}}

### 色彩层次与语义

{{color_semantics}}

## Typography

{{typography_description}}

- **标题字体 (`{{heading_font}}`)**：{{heading_font_usage}}
- **正文字体 (`{{body_font}}`)**：{{body_font_usage}}

### 字体层级

| 层级 | 用途 | 字号 | 字重 |
|------|------|------|------|
| H1 | 页面主标题 | 48px | 700 |
| H2 | 章节标题 | 36px | 600 |
| H3 | 小节标题 | 24px | 600 |
| Body | 正文内容 | 16px | 400 |
| Caption | 辅助说明 | 14px | 400 |

## Layout

{{layout_description}}

### 间距系统

采用 **{{spacing_system}}** 间距原则，基础单位为 8px，支持 4px 半步微调。

- **组件内边距**：16px - 24px
- **卡片间距**：24px
- **页面边距**：32px（移动端 16px）

### 响应式断点

{{responsive_breakpoints}}

## Elevation & Depth

{{elevation_description}}

## Shapes

{{shapes_description}}

- **按钮圆角**：8px
- **卡片圆角**：12px
- **输入框圆角**：4px
- **标签圆角**：9999px（全圆角）

## Components

### Buttons（按钮）

{{button_guidelines}}

### Cards（卡片）

{{card_guidelines}}

### Inputs（输入框）

{{input_guidelines}}

### 其他组件

{{other_components}}

## Do's and Don'ts

### ✅ Do

{{design_dos}}

### ❌ Don't

{{design_donts}}

## 关联文档

- [Spec](spec.md) - 产品需求
- [Architecture](architecture.md) - 技术架构
- [Decisions](../../memory/decisions.md) - 设计决策记录
